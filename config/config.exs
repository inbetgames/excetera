use Mix.Config

# Set the production URL in the parent project like this
#config :excetera, etcd_url: "http://my.host.name:8080/v2/keys"

# Adjust default timeout used for HTTP requests
#config :excetera, timeout: 1000
